name: forge_state
description: State management, secure storage, and caching layer
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  forge_core:
    path: ../forge_core
  forge_backend:
    path: ../forge_backend
  flutter_riverpod: ^2.5.1
  riverpod: ^2.5.1
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter