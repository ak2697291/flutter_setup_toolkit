name: forge_analytics
description: Unified analytics — PostHog, Mixpanel, Firebase Analytics, Amplitude
version: 1.0.0
publish_to: none
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"
dependencies:
  flutter:
    sdk: flutter
  forge_core:
    path: ../forge_core
  # posthog_flutter: ^4.0.1
  # mixpanel_flutter: ^2.3.0
  # firebase_analytics: ^10.8.0
dev_dependencies:
  flutter_test:
    sdk: flutter
