name: forge_payments
description: Unified payment gateway abstraction — Razorpay, Stripe, IAP
version: 1.0.0
publish_to: none
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"
dependencies:
  flutter:
    sdk: flutter
  forge_core:
    path: ../forge_core
  razorpay_flutter: ^1.3.5
  flutter_stripe: ^10.0.0
  in_app_purchase: ^3.1.11
  dartz: ^0.10.1
  http: ^1.6.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
