name: forge_core
description: Core foundation for FlutterForge — DI, routing, theming, environment config
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.6.0
  go_router: ^12.0.0
  flutter_riverpod: ^2.4.0
  riverpod: ^2.4.0
  envied: ^0.5.0
  logger: ^2.0.0
  equatable: ^2.0.5
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  envied_generator: ^0.5.0
  mocktail: ^1.0.0
