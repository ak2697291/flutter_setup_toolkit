name: forge_cli
description: FlutterForge CLI — scaffold new projects from forge.yaml
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.5.0
  yaml: ^3.1.2
  path: ^1.9.0
  mason_logger: ^0.2.15
  process_run: ^0.14.1
  ansi_styles: ^0.3.1

executables:
  forge: forge

dev_dependencies:
  test: ^1.25.7
